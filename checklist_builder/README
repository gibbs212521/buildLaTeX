INSTRUCTIONS:

ATTENTION:
Follow the test_checklist.csv example in directory py_files\test\db\ to see the format
recommended in the .csv files

1. Create CSV checklist generation files in the .\csv_files directory
2. Run the appropriate command from you CLI (Command Line Interface)
   a. "nmake" → Creates a pdf of each .csv file in .\csv_files
   b. "nmake render" → creates and opens each pdf in the .\tex_files
